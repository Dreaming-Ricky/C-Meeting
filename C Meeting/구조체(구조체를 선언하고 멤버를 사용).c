/*#include <stdio.h>

struct student { // 구조체 선언
	int num; // int형 멤버 (학번)
	double grade; // double형 멤버 (학점)
};

int main() {
	struct student s1; // struct student형의 변수 s1 선언
	
	s1.num = 2; // s1의 num 멤버에 2저장
	s1.grade = 2.7; // s1의 grade 멤버에 2.7저장
	printf("학번 : %d\n", s1.num);
	printf("학점 : %.1f\n", s1.grade);

	return 0;	
}*/

